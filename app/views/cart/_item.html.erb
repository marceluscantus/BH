<%= link_to item.glasses.name, :action => 'show', :controller => 'catalog', :id => item.glasses.id %>

<%= pluralize item.amount, "gafa", "gafas" %>, <%= sprintf "%0.2f €", item.price * item.amount %>

(<%= link_to '<b>-</b>'.html_safe, :controller => 'cart', :action => 'remove', :id => item.glasses %>)
